// =============================================================================
// Feature Flags
// Toggle services on/off without removing any code.
// Set to `false` to hide from nav, homepage, pricing, and redirect the page.
// =============================================================================

export const features = {
  weddings: true,
  realEstate: true,
  portraits: true,
}
